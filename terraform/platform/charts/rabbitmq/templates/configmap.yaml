apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rabbitmq.fullname" . }}-config
  labels:
    {{- include "rabbitmq.labels" . | nindent 4 }}
data:
  enabled_plugins: |
    [rabbitmq_management,rabbitmq_peer_discovery_k8s,rabbitmq_prometheus].
  rabbitmq.conf: |
    cluster_name = {{ .Release.Name }}
    cluster_formation.peer_discovery_backend = k8s
    cluster_partition_handling = {{ .Values.cluster.partitionHandling }}
    default_queue_type = {{ .Values.cluster.defaultQueueType }}
    listeners.tcp.default = {{ .Values.service.amqpPort }}
    management.tcp.port = {{ .Values.service.managementPort }}
    prometheus.tcp.port = {{ .Values.service.metricsPort }}
