apiVersion: v1
kind: Service
metadata:
  name: {{ include "etcd-workbench.fullname" . }}
  labels:
    {{- include "etcd-workbench.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "etcd-workbench.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
